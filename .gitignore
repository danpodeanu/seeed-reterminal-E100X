sync.sh

__pycache__/
*.pyc

# Local staging folder for generated .vlw fonts (see tools/fonts/make_vlw.py).
/out/

# Wi-Fi / QWeather credentials header. Optional at build time -- copy the
# matching secrets.h.example if you want compile-time defaults for
# automated provisioning; otherwise configure via the on-device portal.
# Kept out of version control so real credentials never leak.
**/include/secrets.h
